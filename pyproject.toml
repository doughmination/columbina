[project]
name = "columbina"
version = "0.0.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Clove Twilight", email = "admin@doughmination.win" }
]
requires-python = ">=3.14"
dependencies = [
    "colorful>=0.5.8",
    "discord-py>=2.7.1",
    "python-dotenvx>=2.0.0",
    "ruff>=0.16.7",
    "toml>=0.10.2",
    "ty>=0.0.80",
]

[tool.uv]
package=false

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]

[tool.ruff]
target-version = "py313"
